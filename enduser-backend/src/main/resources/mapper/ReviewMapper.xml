<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.app.review.mapper.ReviewMapper">

    <!-- 리뷰 등록 -->
    <insert id="insertReview" parameterType="com.project.app.review.dto.ReviewDto" useGeneratedKeys="true" keyProperty="reviewId">
        INSERT INTO review (
            title, content, user_id, user_type, reservation_id, rating, created_at, updated_at, is_visible
        ) VALUES (
            #{title}, #{content}, #{userId}, #{userType}, #{reservationId}, #{rating}, NOW(), NOW(), #{isVisible}
        )
    </insert>

    <!-- 내가 쓴 리뷰 -->
    <select id="selectMyReviewList" parameterType="String" resultType="com.project.app.review.dto.ReviewDto">
        SELECT * FROM review
        WHERE user_id = #{userId} AND is_visible = 1
        ORDER BY created_at DESC
    </select>

    <!-- 예약 ID로 리뷰 조회 -->
    <select id="selectReviewByReservationId" resultType="com.project.app.review.dto.ReviewDto">
        SELECT * FROM review
        WHERE reservation_id = #{reservationId} AND user_id = #{userId} AND is_visible = 1
    </select>

    <!-- 리뷰 상세 조회 -->
    <select id="selectReviewDetail" parameterType="Long" resultType="com.project.app.review.dto.ReviewDto">
        SELECT * FROM review
        WHERE review_id = #{reviewId} AND is_visible = 1
    </select>

    <!-- 본인 여부 체크 -->
    <select id="countByReviewIdAndUserId" resultType="int">
        SELECT COUNT(*) FROM review
        WHERE review_id = #{reviewId} AND user_id = #{userId} AND is_visible = 1
    </select>

    <!-- 리뷰 수정 -->
    <update id="updateReview" parameterType="com.project.app.review.dto.ReviewDto">
        UPDATE review
        SET title = #{title},
            content = #{content},
            rating = #{rating},
            updated_at = NOW()
        WHERE review_id = #{reviewId} AND is_visible = 1
    </update>

    <!-- 리뷰 삭제 (소프트 삭제) -->
    <update id="deleteReview" parameterType="Long">
        UPDATE review
        SET is_visible = 0, updated_at = NOW()
        WHERE review_id = #{reviewId}
    </update>

</mapper>

